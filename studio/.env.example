# Canonical list of Studio environment variables. Copy to .env and fill in.
# Only SANITY_STUDIO_* values are exposed to the Studio build. Neither is a secret.

SANITY_STUDIO_PROJECT_ID=bn544cp3
SANITY_STUDIO_DATASET=production
