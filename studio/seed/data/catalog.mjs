/**
 * Every hand-authored string in the sample content.
 *
 * Nothing here is derived and nothing here is fetched: the resolver attaches a
 * real YouTube video to each lesson by its `query`, and the builder turns this
 * plus the resolved videos into NDJSON. Keeping the two apart is what makes the
 * import reproducible — re-running the build does not re-scrape.
 *
 * Field lengths follow the schema's validation caps (see the prompt), because a
 * document that imports but fails validation in the Studio is worse than one
 * that never imported.
 */

/** A lesson. Slugs are course-scoped; the builder prefixes them with the course slug. */
const l = (slug, title, overview, mechanics, keyPoints, proTip, query) => ({
  slug,
  title,
  overview,
  mechanics,
  keyPoints,
  proTip,
  query,
})

/** A module. Its duration is never stored — it is the sum of these lessons. */
const m = (title, summary, lessons) => ({title, summary, lessons})

export const instructors = [
  {
    slug: 'priya-raghavan',
    name: 'Priya Raghavan',
    expertise: ['React', 'Next.js', 'Web performance', 'Rendering'],
    bio: 'Priya has spent a decade on large React codebases, most of it on the rendering and performance side. She now teaches the parts of the framework people skip: what actually runs on the server, what ships to the browser, and why the difference shows up in a waterfall.',
  },
  {
    slug: 'daniel-okafor',
    name: 'Daniel Okafor',
    expertise: ['TypeScript', 'Developer tooling', 'Monorepos', 'API design'],
    bio: 'Daniel builds the tooling other teams depend on. He is drawn to type systems for an unglamorous reason: a type that is right is a test nobody has to write. His courses lean hard on the boundary between a program and the messy data it is handed.',
  },
  {
    slug: 'elena-vasquez',
    name: 'Elena Vasquez',
    expertise: ['Python', 'FastAPI', 'PostgreSQL', 'Data pipelines'],
    bio: 'Elena is a backend engineer who has carried the pager for services that read and write a lot of rows. She teaches Python and SQL the way she uses them — with the query plan open in the next window.',
  },
  {
    slug: 'marcus-bright',
    name: 'Marcus Bright',
    expertise: ['Algorithms', 'Data structures', 'Competitive programming', 'Interview prep'],
    bio: 'Marcus has coached hundreds of engineers through algorithm interviews and is unsentimental about what works: recognising the shape of a problem beats memorising a hundred solutions. He builds each topic from the invariant up.',
  },
  {
    slug: 'hana-sato',
    name: 'Hana Sato',
    expertise: ['Machine learning', 'PyTorch', 'LLMs', 'Evaluation', 'RAG'],
    bio: 'Hana works on applied machine learning and spends most of her time on the unfashionable half — data quality and evaluation. She teaches models from the arithmetic outwards, so that the library call is the last step rather than the first.',
  },
  {
    slug: 'tobi-lindqvist',
    name: 'Tobi Lindqvist',
    expertise: ['Distributed systems', 'System design', 'Reliability', 'Caching'],
    bio: 'Tobi has designed and broken enough distributed systems to distrust any diagram without a failure mode on it. His teaching centres on trade-offs: every box you add buys something and costs something, and naming both is the job.',
  },
]

export const categories = [
  {
    slug: 'web-development',
    title: 'Web Development',
    description: 'Building for the browser: rendering, routing, state and the performance work that keeps a real application fast.',
  },
  {
    slug: 'languages-and-tooling',
    title: 'Languages & Tooling',
    description: 'The languages you write every day and the tooling around them — type systems, packaging, testing and build configuration.',
  },
  {
    slug: 'algorithms-and-data-structures',
    title: 'Algorithms & Data Structures',
    description: 'The core toolkit: complexity, the structures worth knowing cold, and the problem patterns that keep reappearing.',
  },
  {
    slug: 'ai-and-machine-learning',
    title: 'AI & Machine Learning',
    description: 'From the maths under a linear model to shipping an application built on a large language model, with evaluation taken seriously.',
  },
  {
    slug: 'backend-and-systems',
    title: 'Backend & Systems',
    description: 'Designing services that survive load and failure: scaling, caching, queues and the trade-offs behind each one.',
  },
  {
    slug: 'data-and-databases',
    title: 'Data & Databases',
    description: 'Querying and operating relational databases, with an emphasis on indexes, query plans and changing schemas safely.',
  },
]

export const courses = [
  {
    slug: 'nextjs-app-router-in-depth',
    title: 'Next.js App Router in Depth',
    summary:
      'Learn the App Router as a rendering model rather than a folder convention: what runs on the server, what reaches the browser, how caching decides freshness, and how mutations get back to your data.',
    instructor: 'priya-raghavan',
    category: 'web-development',
    level: 'intermediate',
    price: 89,
    popular: true,
    outcomes: [
      ['layers', 'Route with confidence', 'Lay out nested layouts, dynamic segments, route groups and parallel routes without guessing.'],
      ['bolt', 'Own the server/client line', 'Decide what is a Server Component, what needs "use client", and what crosses between them.'],
      ['rocket', 'Control caching', 'Choose per request, revalidated or static rendering deliberately instead of inheriting a default.'],
      ['shield', 'Mutate safely', 'Write Server Actions that validate input, revalidate the right cache tags and report errors back.'],
    ],
    docs: [
      ['documentation', 'Next.js App Router documentation', 'The routing, rendering and caching reference this course follows.', 'https://nextjs.org/docs/app'],
      ['documentation', 'React Server Components RFC', 'The original design document behind the server/client split.', 'https://react.dev/reference/rsc/server-components'],
    ],
    modules: [
      m(
        'Routing foundations',
        'How a directory of files becomes a URL space: nested layouts, dynamic segments, and the routing conventions that shape everything after.',
        [
          l('file-based-routing-and-layouts', 'File-Based Routing and Layouts',
            'A folder becomes a route segment and a layout wraps every page beneath it, so shared chrome is declared once instead of imported everywhere.',
            'Each segment can hold a page, a layout, a loading state and an error boundary, and the router composes them from the root down.',
            ['Map a folder tree onto a URL space', 'Share chrome with a nested layout', 'Add loading and error states per segment'],
            'A layout keeps its state when you navigate between the pages inside it — put anything expensive to rebuild, like a sidebar, in the layout.',
            'next.js app router file based routing layouts tutorial'),
          l('dynamic-routes-and-params', 'Dynamic Routes and Route Params',
            'A bracketed folder captures part of the URL as a parameter, which is how one file serves every course, lesson or user page.',
            'The segment value arrives as a param on the page, and catch-all segments collect the rest of the path into an array.',
            ['Capture a URL segment as a typed param', 'Choose between single, catch-all and optional segments', 'Pre-generate pages from known slugs'],
            'Generate your static params from the same query the page uses, so a new record cannot produce a route you never built.',
            'next.js dynamic routes params tutorial'),
          l('route-groups-and-parallel-routes', 'Route Groups and Parallel Routes',
            'Route groups organise files without touching the URL, and parallel routes render two independent trees into one layout at once.',
            'A parenthesised folder is stripped from the path, while a slot folder is passed into the layout as its own prop with its own loading state.',
            ['Reorganise files without changing URLs', 'Give one layout two independent slots', 'Intercept a route to show it as a modal'],
            'Parallel routes are what make a modal that survives a refresh: the same URL renders either the slot or the full page.',
            'next.js route groups parallel routes intercepting tutorial'),
        ]),
      m(
        'Server and client boundaries',
        'The line that defines the App Router: which components execute on the server, which ship to the browser, and what can legally cross between them.',
        [
          l('server-components-explained', 'Server Components Explained',
            'A Server Component runs only on the server and never ships its JavaScript, so the code that talks to your database never reaches a browser.',
            'The server renders the tree, serialises the result into a stream, and the browser only hydrates the client islands inside it.',
            ['Tell a server component from a client one', 'Read data directly inside a component', 'Keep secrets and heavy dependencies off the client'],
            'Everything is a Server Component until you say otherwise — add "use client" only when you reach for state, effects or an event handler.',
            'react server components explained next.js'),
          l('use-client-and-hydration', '"use client" and Hydration',
            'The "use client" directive marks the boundary where server-rendered HTML gets wired up to interactive JavaScript in the browser.',
            'Everything imported below that boundary joins the client bundle, and mismatches between server and client output surface as hydration errors.',
            ['Place the boundary as low in the tree as possible', 'Read a hydration mismatch and fix its cause', 'See what a directive adds to the bundle'],
            'Hydration errors almost always come from rendering something the server cannot know — a date, a random value, or window.',
            'next.js use client directive hydration explained'),
          l('crossing-the-server-client-boundary', 'Passing Data Across the Boundary',
            'Props handed from a Server Component to a Client Component must be serialisable, which quietly rules out functions, classes and dates.',
            'Values are serialised into the RSC payload, so the fix for a non-serialisable prop is usually to pass an id and let the client fetch or render from it.',
            ['Recognise what can and cannot be serialised', 'Pass server-rendered children into a client component', 'Avoid pulling a whole tree onto the client by accident'],
            'Passing a Server Component as children to a Client Component keeps it on the server — the client component never renders it, it only positions it.',
            'next.js passing props server to client components serializable'),
        ]),
      m(
        'Data fetching and caching',
        'Where data is read, how long an answer is considered fresh, and how streaming lets a page respond before every query has finished.',
        [
          l('fetching-in-server-components', 'Fetching in Server Components',
            'A Server Component can await its data directly, which removes the effect-plus-state-plus-loading-flag dance entirely.',
            'Requests run on the server close to your data, and sibling fetches should be started together so they overlap instead of queueing.',
            ['Await data inside a component', 'Start parallel requests instead of waterfalls', 'Keep the read token on the server'],
            'Two awaits in sequence is a waterfall. Kick both promises off first and await them together when neither depends on the other.',
            'next.js data fetching server components tutorial'),
          l('caching-and-revalidation', 'Caching and Revalidation',
            'Caching decides how stale an answer may be, and the App Router lets you set that per request rather than per application.',
            'A cached response can be given a lifetime or a set of tags, and revalidating a tag invalidates every response that carried it.',
            ['Choose static, revalidated or per-request rendering', 'Tag responses so one write can invalidate many', 'Diagnose a page that serves stale data'],
            'Tag by document type and by document id. A webhook can then revalidate exactly what changed instead of the whole route.',
            'next.js caching revalidation revalidateTag tutorial'),
          l('streaming-with-suspense', 'Streaming with Suspense',
            'Streaming sends the shell of a page immediately and fills slow regions in as their data arrives, instead of blocking on the slowest query.',
            'A Suspense boundary marks a region that may arrive late, and its fallback is what the user sees in the meantime.',
            ['Wrap a slow region in a Suspense boundary', 'Design a fallback that does not shift layout', 'Decide what belongs above the fold and outside a boundary'],
            'Give the fallback the same height as the real content — a stream that reflows the page is worse than one that waits.',
            'next.js streaming suspense loading ui tutorial'),
        ]),
      m(
        'Mutations and production',
        'Getting writes from a form back into your data with validation and cache invalidation, then putting the result into production.',
        [
          l('server-actions', 'Server Actions',
            'A Server Action is a function that runs on the server but is called from the client, so a write needs no hand-written API route.',
            'The framework creates the endpoint, serialises arguments across it, and lets the action revalidate affected caches before it returns.',
            ['Write a mutation without an API route', 'Revalidate the cache the write affected', 'Treat every action argument as untrusted input'],
            'A Server Action is a public endpoint. Re-check authorisation inside it — the fact that the button was hidden proves nothing.',
            'next.js server actions tutorial'),
          l('forms-and-validation', 'Forms and Validation',
            'Progressive forms post to a Server Action and report field errors back, working before any JavaScript has loaded.',
            'Form state hooks carry the action result back into the UI, and a schema validates the submission on the server where it counts.',
            ['Wire a form to a server action', 'Return field errors and render them', 'Show pending state while a submission is in flight'],
            'Validate with the same schema on both sides if you like, but treat the server run as the only one that decides.',
            'next.js form validation server actions useActionState'),
          l('deploying-to-production', 'Deploying to Production',
            'Deployment is where rendering decisions become real: what was static, what runs per request, and what a cold start actually costs.',
            'Environment variables split into server-only secrets and public values, and the build output tells you which routes were prerendered.',
            ['Read the build output per route', 'Separate server secrets from public config', 'Set up revalidation in a deployed environment'],
            'Check the build summary before every release. A route that silently became dynamic is the most common cause of a sudden bill.',
            'next.js deploy production build output environment variables'),
        ]),
    ],
  },

  {
    slug: 'react-performance-engineering',
    title: 'React Performance Engineering',
    summary:
      'Make a React application fast on purpose: measure before you optimise, cut the renders that do nothing, ship less JavaScript, and keep the main thread free when the work is genuinely heavy.',
    instructor: 'priya-raghavan',
    category: 'web-development',
    level: 'advanced',
    price: 99,
    popular: false,
    outcomes: [
      ['chart', 'Measure first', 'Use the Profiler and field metrics to find the actual bottleneck instead of guessing.'],
      ['bolt', 'Cut wasted renders', 'Apply memoisation where it pays and recognise where it is pure overhead.'],
      ['rocket', 'Ship less JavaScript', 'Split bundles, analyse what is in them, and load media without blocking paint.'],
      ['layers', 'Scale the UI', 'Virtualise long lists and move heavy work off the main thread.'],
    ],
    docs: [
      ['documentation', 'React performance documentation', 'Official guidance on memoisation, transitions and profiling.', 'https://react.dev/learn/render-and-commit'],
      ['documentation', 'web.dev Core Web Vitals', 'Definitions and thresholds for LCP, INP and CLS.', 'https://web.dev/articles/vitals'],
    ],
    modules: [
      m(
        'Measure before you optimise',
        'Finding the real bottleneck with the Profiler and field metrics, so the work you do afterwards is aimed at something that matters.',
        [
          l('react-profiler', 'Profiling with the React Profiler',
            'The Profiler records every commit and shows which components rendered, how long they took, and what triggered them.',
            'A flame chart per commit turns a vague "it feels slow" into a named component with a measured cost.',
            ['Record and read a profiling session', 'Find the component that dominates a commit', 'Attribute a render to the state that caused it'],
            'Profile a production build. A development build carries checks and warnings that distort every number you are about to act on.',
            'react profiler devtools performance tutorial'),
          l('core-web-vitals', 'Core Web Vitals in a React App',
            'Lab numbers describe your machine; Core Web Vitals describe your users, and the two often disagree.',
            'LCP tracks when the main content paints, INP tracks interaction responsiveness, and CLS tracks visual stability.',
            ['Interpret LCP, INP and CLS', 'Collect field data rather than lab data alone', 'Tie a regression back to a release'],
            'INP is usually a main-thread problem, not a network one — look at what your event handlers do before you look at your bundle.',
            'core web vitals lcp inp cls explained web performance'),
          l('finding-wasted-renders', 'Finding Wasted Renders',
            'A wasted render is one that produces identical output, and a component tree can produce hundreds of them per interaction.',
            'Highlighting updates shows re-renders visually, and the usual culprits are new object identities created during render.',
            ['Spot renders that change nothing', 'Trace a re-render to a changing prop identity', 'Separate a real cost from a harmless one'],
            'Not every extra render is a bug. A cheap component rendering twice costs less than the memoisation you were about to add.',
            'react wasted renders re-render debugging tutorial'),
        ]),
      m(
        'Render optimisation',
        'Memoisation, context and list reconciliation — the three places where an avoidable render usually comes from.',
        [
          l('memo-usememo-usecallback', 'memo, useMemo and useCallback',
            'These three tools all trade memory and complexity for skipped work, and each is only worth it when the skipped work is real.',
            'memo compares props, useMemo caches a computed value, and useCallback keeps a function identity stable across renders.',
            ['Choose the right memoisation tool', 'Keep prop identity stable so memo can work', 'Recognise memoisation that costs more than it saves'],
            'memo on a component whose props are a fresh object every render does nothing but add a comparison — fix the identity first.',
            'react memo usememo usecallback explained tutorial'),
          l('context-without-rerenders', 'Context Without Re-Renders',
            'Every consumer of a context re-renders when its value changes, which turns one shared object into an application-wide update.',
            'Splitting a context into stable and volatile halves, and memoising the value object, keeps unrelated consumers still.',
            ['See why one context update re-renders everything', 'Split a context by change frequency', 'Memoise a provider value correctly'],
            'Put the setters in one context and the state in another. Components that only dispatch then stop re-rendering entirely.',
            'react context re-render optimization split context'),
          l('keys-and-reconciliation', 'Keys and List Reconciliation',
            'A key tells React which item is which, and the wrong key makes it destroy and rebuild rows that only moved.',
            'Reconciliation matches elements by position and key, so an index key breaks as soon as the list is reordered or filtered.',
            ['Choose a key that identifies the item', 'Explain why index keys lose state', 'Reset component state deliberately with a key'],
            'Changing a key is also the cleanest way to reset a subtree — a controlled remount beats a pile of clearing effects.',
            'react keys reconciliation list rendering explained'),
        ]),
      m(
        'Loading performance',
        'Reducing what the browser must download and parse before a page becomes usable.',
        [
          l('code-splitting-and-lazy', 'Code Splitting and Lazy Loading',
            'Code splitting defers the JavaScript a user may never need, so the first screen does not pay for the whole application.',
            'A lazy import becomes its own chunk, fetched on demand and displayed behind a Suspense fallback.',
            ['Split a route or a heavy component', 'Pair a lazy import with a fallback', 'Prefetch a chunk before it is needed'],
            'Split at routes and at genuinely heavy leaves — an editor, a chart library. Splitting small components adds requests and saves nothing.',
            'react code splitting lazy suspense tutorial'),
          l('bundle-analysis', 'Bundle Analysis',
            'A bundle analyser shows what is actually in your JavaScript, which is usually a surprise and often a date library.',
            'The treemap attributes bytes to modules, exposing duplicated dependencies and libraries that resisted tree-shaking.',
            ['Read a bundle treemap', 'Find duplicated or oversized dependencies', 'Replace a heavy dependency with a lighter path'],
            'Check for two versions of the same package before you optimise anything else — deduplicating is free and often the biggest win.',
            'javascript bundle analyzer webpack bundle size tutorial'),
          l('image-and-font-loading', 'Image and Font Loading',
            'Images and fonts usually decide when a page looks ready, and both are easy to load in a way that blocks or shifts the layout.',
            'Sizing images up front prevents layout shift, and a font display strategy decides whether text is invisible or swapped.',
            ['Size media to prevent layout shift', 'Choose modern image formats and responsive sources', 'Preload the font that renders the headline'],
            'Reserve the space before the asset arrives. Most layout shift is an element that had no dimensions until its image loaded.',
            'web performance image optimization font loading cls tutorial'),
        ]),
      m(
        'Advanced runtime work',
        'Techniques for when the work itself is large: long lists, urgent versus deferred updates, and moving computation off the main thread.',
        [
          l('virtualizing-long-lists', 'Virtualising Long Lists',
            'Virtualisation renders only the rows that fit on screen, so a ten-thousand-row table costs about as much as a twenty-row one.',
            'A windowing library tracks scroll position and item heights, mounting a moving slice of the list and spacing the rest.',
            ['Render a windowed slice of a long list', 'Handle variable row heights', 'Keep keyboard navigation and search working'],
            'Virtualisation breaks in-page find and anchor links. If a list needs to be searchable by the browser, paginate instead.',
            'react virtualized list windowing tutorial'),
          l('concurrent-rendering-and-transitions', 'Concurrent Rendering and Transitions',
            'Marking an update as a transition tells React it may be interrupted, which keeps typing responsive while an expensive list re-renders.',
            'Urgent updates render immediately while transition updates yield to input, and a deferred value trails behind on purpose.',
            ['Separate urgent from non-urgent updates', 'Keep an input responsive during heavy rendering', 'Show pending state for a transition'],
            'Transitions do not make the work cheaper; they make it interruptible. If a render takes a second, that second is still spent.',
            'react concurrent rendering usetransition usedeferredvalue tutorial'),
          l('web-workers-for-heavy-work', 'Web Workers for Heavy Work',
            'A Web Worker runs JavaScript on another thread, which is the only real fix when a computation genuinely takes hundreds of milliseconds.',
            'The worker communicates by messages, so data is copied across unless it is explicitly transferred.',
            ['Move a heavy computation off the main thread', 'Pass data to a worker without copying it twice', 'Decide when a worker is worth its complexity'],
            'Measure the message cost first. Serialising a large array can be more expensive than the calculation you moved.',
            'javascript web workers tutorial offload main thread'),
        ]),
    ],
  },

  {
    slug: 'typescript-at-scale',
    title: 'TypeScript at Scale',
    summary:
      'Use the type system as leverage on a large codebase: narrow unions properly, write generics that stay readable, validate data at the boundary, and configure a project so the compiler catches real mistakes.',
    instructor: 'daniel-okafor',
    category: 'languages-and-tooling',
    level: 'intermediate',
    price: 79,
    popular: true,
    outcomes: [
      ['terminal', 'Model data honestly', 'Describe what your data actually is with unions, narrowing and discriminants.'],
      ['layers', 'Write reusable types', 'Build generic, conditional and mapped types that stay legible months later.'],
      ['shield', 'Guard the boundary', 'Validate untrusted input at runtime and keep the inferred type in step with it.'],
      ['bolt', 'Configure for scale', 'Set strictness, wire up project references, and make type checking part of CI.'],
    ],
    docs: [
      ['documentation', 'TypeScript handbook', 'The reference for every language feature covered in this course.', 'https://www.typescriptlang.org/docs/handbook/intro.html'],
      ['documentation', 'tsconfig reference', 'Every compiler option, with its default and its effect.', 'https://www.typescriptlang.org/tsconfig'],
    ],
    modules: [
      m(
        'How the type system thinks',
        'Structural typing, narrowing and generics — the three ideas the rest of the language is built on.',
        [
          l('structural-typing', 'Structural Typing',
            'TypeScript compares shapes, not names, so anything with the right properties is assignable regardless of where it came from.',
            'Assignability is checked property by property, which is why an extra property is fine from a variable but rejected from a literal.',
            ['Explain why two unrelated types are compatible', 'Predict when excess property checks fire', 'Use branded types when a shape is not enough'],
            'When two shapes should not be interchangeable — a UserId and an OrderId — brand them, or the compiler will happily swap the two.',
            'typescript structural typing explained'),
          l('unions-and-narrowing', 'Unions and Narrowing',
            'A union says a value is one of several things, and narrowing is how you prove which one before you use it.',
            'Type guards, discriminant properties and exhaustive switches let the compiler follow the same reasoning you are doing.',
            ['Model alternatives with a discriminated union', 'Narrow with typeof, in and custom guards', 'Make a switch exhaustive with never'],
            'Give every variant a literal discriminant field. It makes narrowing trivial and turns a missing case into a compile error.',
            'typescript discriminated unions narrowing tutorial'),
          l('generics', 'Generics That Stay Readable',
            'A generic keeps the relationship between an input type and an output type instead of erasing both to any.',
            'Type parameters flow through a function, and constraints limit them enough to be useful without overspecifying.',
            ['Write a generic function that preserves types', 'Constrain a type parameter with extends', 'Know when a generic is not earning its keep'],
            'If a type parameter appears exactly once in a signature, it is not relating anything — that is a sign it should not be generic.',
            'typescript generics tutorial constraints'),
        ]),
      m(
        'Advanced type construction',
        'Types that compute: deriving one type from another instead of maintaining both by hand.',
        [
          l('conditional-types', 'Conditional Types',
            'A conditional type chooses between two types based on a relationship, which is how a library adapts to whatever you pass it.',
            'Inference inside a conditional extracts part of a type, and distribution applies the condition to each member of a union.',
            ['Branch a type on an assignability check', 'Extract inner types with infer', 'Control whether a conditional distributes'],
            'Distribution over unions surprises people constantly. Wrap the checked type in a tuple when you want it treated as one unit.',
            'typescript conditional types infer explained'),
          l('mapped-types', 'Mapped Types',
            'A mapped type rewrites the keys of an existing type, which is how Partial, Readonly and Record are all defined.',
            'Iterating over keys lets you add or remove modifiers and remap names, deriving a variant instead of duplicating the original.',
            ['Transform every property of a type', 'Add or strip optional and readonly modifiers', 'Rename keys with an as clause'],
            'Derive DTOs from your domain type with a mapped type. Two hand-written copies drift; a derived one cannot.',
            'typescript mapped types tutorial'),
          l('template-literal-types', 'Template Literal Types',
            'Template literal types build string types from other types, giving structured strings like route paths real checking.',
            'Combining them with unions and inference lets the compiler parse a string pattern at the type level.',
            ['Compose string types from unions', 'Type an event or route naming convention', 'Parse a pattern with inference'],
            'Great for a fixed vocabulary like event names. Keep it away from genuinely open strings — the union explodes fast.',
            'typescript template literal types tutorial'),
        ]),
      m(
        'Types at the boundary',
        'The edge where data arrives from outside the program, and where a type is only a claim until something checks it.',
        [
          l('runtime-validation-with-zod', 'Runtime Validation with Zod',
            'A type annotation disappears at runtime, so data crossing into your program needs an actual check, not a cast.',
            'A schema validates a value and infers its static type, keeping the runtime guarantee and the compile-time type in step.',
            ['Validate untrusted input against a schema', 'Infer a static type from a schema', 'Handle and report validation failures'],
            'Parse once at the edge and pass the parsed value inward. Validating repeatedly in the middle of a program means you never trusted the edge.',
            'zod runtime validation typescript tutorial'),
          l('typing-api-responses', 'Typing API Responses',
            'A response body is unknown until proven otherwise, and calling it a typed object is the most common lie in a codebase.',
            'Starting from unknown forces a validation step before the value is usable, which is exactly the point.',
            ['Start from unknown instead of any', 'Validate a response before using it', 'Keep generated types in sync with a live API'],
            'Generated API types are a description, not a guarantee. The server can still send something else on a bad day.',
            'typescript typing api responses unknown fetch tutorial'),
          l('declaration-files', 'Declaration Files',
            'A declaration file describes the shape of JavaScript that has no types of its own, including your own untyped modules.',
            'Ambient declarations, module augmentation and global types let you describe or extend code you do not control.',
            ['Write a declaration for an untyped package', 'Augment an existing module type', 'Declare globals and asset modules'],
            'Augment a third-party module rather than forking its types — an upgrade will then tell you if your assumption broke.',
            'typescript declaration files d.ts module augmentation tutorial'),
        ]),
      m(
        'Configuration and tooling',
        'Compiler settings, multi-package builds and the CI step that makes any of it binding.',
        [
          l('tsconfig-strictness', 'tsconfig and Strictness',
            'Compiler options decide how much the type system is actually allowed to tell you, and the defaults are lenient.',
            'Strict mode bundles several checks, of which null checking is the one that finds the most real bugs.',
            ['Understand what strict actually turns on', 'Adopt strictness incrementally in a large codebase', 'Choose module and target settings deliberately'],
            'Turn strict on for new folders first with a scoped config. A full-repo flip that produces 4,000 errors gets reverted.',
            'typescript tsconfig strict mode explained'),
          l('project-references', 'Monorepo Project References',
            'Project references split a large codebase into buildable units so the compiler does incremental work instead of full rebuilds.',
            'Each package emits declaration files that its dependents consume, which makes the dependency graph explicit and enforceable.',
            ['Split a monorepo into referenced projects', 'Build incrementally with composite projects', 'Debug a stale declaration output'],
            'Project references enforce your architecture: a package that is not referenced cannot be imported, accidentally or otherwise.',
            'typescript project references monorepo tutorial'),
          l('type-checking-in-ci', 'Type Checking in CI',
            'A type error that only appears on a developer machine is optional; one that fails the build is not.',
            'Running the compiler with no emit in CI separates type correctness from bundling, which is usually done by a different tool.',
            ['Run a type check as its own CI step', 'Keep the editor and CI on the same version', 'Speed up checking on a large repo'],
            'Bundlers usually strip types without checking them. If CI does not run the compiler, nothing does.',
            'typescript github actions ci pipeline tutorial'),
        ]),
    ],
  },

  {
    slug: 'modern-python-for-backend-engineers',
    title: 'Modern Python for Backend Engineers',
    summary:
      'Python as it is actually written on a backend team: the data model underneath the syntax, async concurrency and its traps, typed APIs with FastAPI, and the testing and packaging work that gets it into production.',
    instructor: 'elena-vasquez',
    category: 'languages-and-tooling',
    level: 'beginner',
    price: 69,
    popular: false,
    outcomes: [
      ['terminal', 'Know the data model', 'Understand what Python objects really are and why mutability keeps surprising people.'],
      ['bolt', 'Write async that works', 'Use asyncio deliberately and recognise the blocking call that ruins it.'],
      ['rocket', 'Build a typed API', 'Serve validated, documented endpoints with FastAPI and Pydantic.'],
      ['shield', 'Ship it', 'Test with pytest, query with SQLAlchemy and package the service in a container.'],
    ],
    docs: [
      ['documentation', 'Python standard library reference', 'The library documentation this course leans on throughout.', 'https://docs.python.org/3/library/index.html'],
      ['documentation', 'FastAPI documentation', 'Routing, dependencies and validation for the API modules.', 'https://fastapi.tiangolo.com/'],
    ],
    modules: [
      m(
        'Python foundations',
        'The object model, the iteration protocol and the environment tooling that every later module assumes you have.',
        [
          l('types-and-the-data-model', 'Types and the Data Model',
            'Every value in Python is an object, and names are labels bound to objects rather than boxes holding them.',
            'Assignment binds a name, mutability decides whether a change is visible elsewhere, and dunder methods define behaviour.',
            ['Explain the difference between a name and an object', 'Predict when a mutation is shared', 'Implement behaviour with dunder methods'],
            'Never use a mutable default argument. It is created once at definition time and then shared by every call.',
            'python data model objects mutability explained'),
          l('comprehensions-and-iterators', 'Comprehensions and Iterators',
            'Iteration in Python is a protocol, and generators let you produce values lazily instead of building a list you never fully need.',
            'A comprehension builds a collection eagerly while a generator yields one item at a time, keeping memory flat.',
            ['Write readable comprehensions', 'Stream large data with a generator', 'Use the iterator protocol directly'],
            'Reach for a generator as soon as the collection might be large — a comprehension over a million rows materialises all of them.',
            'python generators iterators comprehensions tutorial'),
          l('virtual-environments-and-packaging', 'Virtual Environments and Packaging',
            'A virtual environment isolates a project dependencies so two projects can disagree about a library version in peace.',
            'A project file declares dependencies while a lock file pins the exact resolved versions that were tested.',
            ['Create and use an isolated environment', 'Declare dependencies in a project file', 'Pin versions with a lock file'],
            'Commit the lock file. Without it, "works on my machine" is a statement about a resolution that happened on a particular Tuesday.',
            'python virtual environments packaging pyproject tutorial'),
        ]),
      m(
        'Async Python',
        'Concurrency with asyncio: what the event loop actually does, the patterns that use it well, and the mistakes that silently serialise everything.',
        [
          l('asyncio-event-loop', 'The asyncio Event Loop',
            'The event loop runs one coroutine at a time and switches at await points, which is concurrency without parallelism.',
            'A coroutine yields control while waiting on IO, letting the loop run other work until the result is ready.',
            ['Describe what happens at an await point', 'Distinguish concurrency from parallelism', 'Run coroutines on the loop correctly'],
            'Async speeds up waiting, not computing. CPU-bound work belongs in a process pool, not a coroutine.',
            'python asyncio event loop explained tutorial'),
          l('async-await-patterns', 'async/await Patterns',
            'Gathering tasks runs requests concurrently, which is the entire reason to write async code in the first place.',
            'Task groups start work in parallel and collect results, with timeouts and cancellation handled as first-class concerns.',
            ['Run many IO calls concurrently', 'Apply timeouts and cancellation', 'Structure tasks so failures propagate cleanly'],
            'Awaiting inside a loop is sequential code wearing an async costume. Create the tasks first, then await them together.',
            'python asyncio gather task group patterns tutorial'),
          l('async-pitfalls', 'Concurrency Pitfalls',
            'One synchronous blocking call inside a coroutine stops the entire event loop, and nothing warns you that it happened.',
            'Blocking IO, unawaited coroutines and shared mutable state are the three failures that account for most async bugs.',
            ['Spot a blocking call inside async code', 'Move blocking work to an executor', 'Avoid fire-and-forget tasks that swallow errors'],
            'Keep a reference to every task you create. A garbage-collected task can vanish mid-flight, taking its exception with it.',
            'python asyncio common mistakes blocking event loop'),
        ]),
      m(
        'Building APIs with FastAPI',
        'Turning functions into a documented, validated HTTP interface, then putting authentication and cross-cutting concerns around it.',
        [
          l('fastapi-basics', 'FastAPI Basics',
            'FastAPI reads your type hints to route, validate and document an endpoint, so the signature is the specification.',
            'Path and query parameters are parsed from the annotations, and dependencies inject shared resources per request.',
            ['Define routes with typed parameters', 'Inject shared resources as dependencies', 'Read the generated API documentation'],
            'Let dependencies own resource lifetimes — a database session that opens and closes around the request is one fewer leak.',
            'fastapi tutorial routing dependencies beginners'),
          l('pydantic-models', 'Pydantic Models',
            'A Pydantic model validates and coerces incoming data at the edge, turning a request body into a typed object or an error.',
            'Validators enforce rules beyond shape, and separate models keep what a client may send apart from what you store.',
            ['Validate request bodies with a model', 'Add custom field and model validators', 'Separate input, storage and response models'],
            'Never reuse your database model as the request body. Otherwise a client can set any field you forgot to exclude.',
            'pydantic models validation tutorial fastapi'),
          l('auth-and-middleware', 'Authentication and Middleware',
            'Authentication decides who is calling, and middleware applies the concerns that every request shares regardless of route.',
            'A dependency can extract and verify a token per route, while middleware handles logging, CORS and request context globally.',
            ['Verify a bearer token as a dependency', 'Apply cross-cutting concerns with middleware', 'Return correct status codes for auth failures'],
            'Authorise inside the handler too. Authentication tells you who it is, not whether they may touch this particular row.',
            'fastapi authentication jwt middleware tutorial'),
        ]),
      m(
        'Production concerns',
        'Testing, database access and containerisation — what stands between a working script and a service you can operate.',
        [
          l('testing-with-pytest', 'Testing with pytest',
            'pytest keeps tests as plain functions, so the barrier to writing one is low enough that people actually do.',
            'Fixtures provide dependencies and clean up after them, and parametrisation covers many cases from one test body.',
            ['Write fixtures for shared setup', 'Parametrise a test across cases', 'Test an API client end to end'],
            'Test behaviour, not implementation. A test that asserts which internal function was called breaks on every refactor.',
            'pytest tutorial fixtures parametrize testing python'),
          l('database-access-with-sqlalchemy', 'Database Access with SQLAlchemy',
            'An ORM maps rows to objects, which is convenient right up until it quietly issues a query per object in a loop.',
            'Sessions define a unit of work, and eager loading strategies decide how many queries a relationship actually costs.',
            ['Model tables and relationships', 'Manage a session as a unit of work', 'Avoid N+1 queries with eager loading'],
            'Log the SQL in development. An ORM that emits 200 queries for one page looks identical in Python to one that emits two.',
            'sqlalchemy tutorial orm session relationships'),
          l('deploying-with-docker', 'Deploying with Docker',
            'A container fixes the interpreter, the dependencies and the system libraries, so the thing you tested is the thing that runs.',
            'A multi-stage build keeps compilation out of the final image, and layer ordering decides how often you rebuild the world.',
            ['Write a small, cacheable image', 'Separate build and runtime stages', 'Configure a service through environment variables'],
            'Copy the dependency files and install before copying your source. Otherwise every code change reinstalls everything.',
            'docker python fastapi production dockerfile tutorial'),
        ]),
    ],
  },

  {
    slug: 'data-structures-foundations',
    title: 'Data Structures Foundations',
    summary:
      'The structures every other course assumes you know, built from the invariant outwards: what each one guarantees, what it costs, and which problems it is the obvious answer to.',
    instructor: 'marcus-bright',
    category: 'algorithms-and-data-structures',
    level: 'beginner',
    price: 59,
    popular: true,
    outcomes: [
      ['chart', 'Reason about cost', 'Analyse time and space complexity without hand-waving.'],
      ['layers', 'Know the structures', 'Understand arrays, lists, stacks, queues, trees, heaps, hash tables and graphs.'],
      ['terminal', 'Implement from scratch', 'Build each structure yourself so its trade-offs are obvious rather than memorised.'],
      ['bolt', 'Pick the right one', 'Match a structure to a problem by the operation it needs to be fast.'],
    ],
    docs: [
      ['article', 'Big-O cheat sheet', 'Complexity of common operations across the structures in this course.', 'https://www.bigocheatsheet.com/'],
      ['documentation', 'Python collections module', 'Deques, counters and the built-in structures referenced in the exercises.', 'https://docs.python.org/3/library/collections.html'],
    ],
    modules: [
      m(
        'Complexity and arrays',
        'How to talk about cost, and the contiguous structure that most other structures are built on top of.',
        [
          l('big-o-notation', 'Big-O Notation',
            'Big-O describes how the cost of an algorithm grows with its input, ignoring the constants that differ between machines.',
            'Analysis counts the dominant repeated operation, which is why nested loops and halving searches have such different shapes.',
            ['Derive the complexity of a loop or recursion', 'Compare growth rates meaningfully', 'Analyse space alongside time'],
            'Constants still matter in practice. An O(n log n) algorithm can lose to an O(n squared) one on the sizes you actually run.',
            'big o notation time complexity explained tutorial'),
          l('arrays-and-dynamic-arrays', 'Arrays and Dynamic Arrays',
            'An array gives constant-time access by index because its elements sit contiguously in memory.',
            'A dynamic array grows by allocating a larger block and copying, which makes an append cheap on average but occasionally expensive.',
            ['Explain why indexing is constant time', 'Describe amortised append cost', 'Predict the cost of inserting in the middle'],
            'Reserve capacity when you know the final size. Repeated growth doubles and copies more times than people expect.',
            'dynamic array resizing amortized analysis explained'),
          l('two-pointer-technique', 'The Two Pointer Technique',
            'Two pointers walk an array from different positions, replacing a nested loop with a single pass.',
            'The technique works when the array is sorted or when a window has a monotonic property you can exploit.',
            ['Recognise a two-pointer problem', 'Solve pair-sum problems in linear time', 'Partition an array in place'],
            'If the array must be sorted first, count the sort in your complexity — linear after an O(n log n) sort is not linear.',
            'two pointer technique arrays algorithm tutorial'),
        ]),
      m(
        'Linked structures',
        'Structures built from nodes and references, and the two access disciplines that follow from them.',
        [
          l('linked-lists', 'Linked Lists',
            'A linked list trades indexing for cheap insertion, since each node only knows where the next one is.',
            'Traversal is linear, and most list bugs are a pointer reassigned in the wrong order during an insert or delete.',
            ['Implement insertion and deletion correctly', 'Reverse a list iteratively', 'Detect a cycle with fast and slow pointers'],
            'Use a dummy head node. It removes the special case for an empty list that causes most of the off-by-one errors.',
            'linked list data structure implementation tutorial'),
          l('stacks', 'Stacks',
            'A stack only lets you touch the most recent item, which is exactly the discipline that matching and backtracking problems need.',
            'Push and pop are constant time, and the call stack itself is the structure applied to function execution.',
            ['Implement a stack over an array or list', 'Solve bracket matching and expression problems', 'Convert recursion into an explicit stack'],
            'A deep recursion that overflows is usually fine as an explicit stack — the algorithm is unchanged, the memory just moves.',
            'stack data structure implementation tutorial'),
          l('queues-and-deques', 'Queues and Deques',
            'A queue serves the oldest item first, which is the order breadth-first traversal and most scheduling problems require.',
            'A circular buffer keeps enqueue and dequeue constant time, and a deque allows both ends without losing that.',
            ['Implement a queue without shifting elements', 'Use a deque for sliding window problems', 'Explain why a naive array queue is slow'],
            'Removing from the front of a plain array is linear. That one detail turns a clean BFS into a quadratic one.',
            'queue deque circular buffer data structure tutorial'),
        ]),
      m(
        'Trees and heaps',
        'Hierarchical structures: traversal orders, the ordering invariant that makes search fast, and the partial order that makes a priority queue possible.',
        [
          l('binary-trees-and-traversal', 'Binary Trees and Traversal',
            'A binary tree branches at most twice per node, and the order you visit those branches changes what the traversal means.',
            'Pre-order, in-order and post-order differ only in when a node is processed relative to its children.',
            ['Implement all three depth-first traversals', 'Traverse level by level', 'Compute depth and balance'],
            'In-order traversal of a search tree yields sorted output — a quick way to check your tree is actually valid.',
            'binary tree traversal inorder preorder postorder tutorial'),
          l('binary-search-trees', 'Binary Search Trees',
            'A search tree keeps everything smaller on the left and larger on the right, which turns lookup into a sequence of decisions.',
            'The invariant gives logarithmic operations only while the tree stays balanced; sorted insertions degrade it to a list.',
            ['Implement search, insert and delete', 'Explain why balance decides performance', 'Handle the two-child deletion case'],
            'Inserting sorted data into an unbalanced tree gives you a linked list with extra steps. That is what self-balancing trees fix.',
            'binary search tree insert delete tutorial'),
          l('heaps-and-priority-queues', 'Heaps and Priority Queues',
            'A heap maintains only enough order to know the minimum or maximum, which is cheaper than keeping everything sorted.',
            'It is stored as an array, with sift-up and sift-down restoring the invariant in logarithmic time after each change.',
            ['Implement a binary heap over an array', 'Push and pop in logarithmic time', 'Use a heap for top-k and scheduling problems'],
            'For top-k over a large stream, keep a heap of size k. Sorting everything to take k items does far more work than needed.',
            'binary heap priority queue implementation tutorial'),
        ]),
      m(
        'Hashing and graphs',
        'Constant-time lookup by key, and the structure that models relationships along with the two ways to walk it.',
        [
          l('hash-tables', 'Hash Tables',
            'A hash table computes where a key belongs instead of searching for it, which is what makes lookup constant on average.',
            'Collisions are inevitable, so the table resolves them by chaining or probing and resizes once it gets too full.',
            ['Implement hashing with collision resolution', 'Explain the load factor and resizing', 'Know when worst case degrades to linear'],
            'A mutable object used as a key is a bug waiting to happen — change it and its bucket no longer matches its hash.',
            'hash table implementation collision resolution tutorial'),
          l('graph-representations', 'Graph Representations',
            'A graph is nodes and edges, and the representation you choose decides which questions are cheap to answer.',
            'An adjacency list is compact for sparse graphs, while a matrix answers "is there an edge" instantly at quadratic space.',
            ['Choose between a list and a matrix', 'Represent weighted and directed edges', 'Model a real problem as a graph'],
            'Most interview graphs are sparse, so an adjacency list is the default. Reach for a matrix only when you query edges constantly.',
            'graph representation adjacency list matrix tutorial'),
          l('bfs-and-dfs', 'Breadth-First and Depth-First Search',
            'Both searches visit every reachable node; the queue or stack they use decides the order and therefore what they are good for.',
            'BFS expands by distance, which finds shortest paths in an unweighted graph, while DFS goes deep and suits connectivity and cycles.',
            ['Implement BFS and DFS over a graph', 'Find the shortest unweighted path', 'Track visited nodes to avoid infinite loops'],
            'Mark a node visited when you enqueue it, not when you dequeue it, or the same node lands in the queue many times.',
            'bfs dfs graph traversal algorithm tutorial'),
        ]),
    ],
  },

  {
    slug: 'algorithmic-problem-patterns',
    title: 'Algorithmic Problem Patterns',
    summary:
      'Stop memorising solutions and start recognising shapes. Binary search on an answer, dynamic programming, the graph algorithms worth knowing, and how to keep your reasoning legible under interview pressure.',
    instructor: 'marcus-bright',
    category: 'algorithms-and-data-structures',
    level: 'advanced',
    price: 89,
    popular: false,
    outcomes: [
      ['rocket', 'See the pattern', 'Classify an unfamiliar problem by its structure rather than its story.'],
      ['layers', 'Master dynamic programming', 'Move from recursion to memoisation to a tabulated solution deliberately.'],
      ['bolt', 'Know the graph toolkit', 'Apply shortest paths, topological order and union-find where each belongs.'],
      ['shield', 'Justify your answer', 'Argue correctness and complexity, not just produce output that passes.'],
    ],
    docs: [
      ['article', 'Competitive programming algorithms', 'Reference implementations and proofs for the algorithms in this course.', 'https://cp-algorithms.com/'],
      ['repository', 'Practice problem set', 'A curated list of problems mapped to each pattern.', 'https://github.com/topics/algorithms'],
    ],
    modules: [
      m(
        'Searching and sorting patterns',
        'Three patterns that reduce a search space: binary search beyond sorted arrays, the divide-and-conquer sorts, and the sliding window.',
        [
          l('binary-search-on-answer', 'Binary Search on the Answer',
            'Binary search works on any monotonic predicate, which means you can search a range of possible answers rather than an array.',
            'You define a feasibility check, prove it is monotonic, and halve the candidate range until one answer remains.',
            ['Recognise a monotonic feasibility predicate', 'Search a numeric answer range', 'Avoid off-by-one errors in the bounds'],
            'Write the predicate first and confirm it is monotonic. If feasibility flips more than once, binary search is simply wrong here.',
            'binary search on answer monotonic predicate tutorial'),
          l('merge-sort-and-quick-sort', 'Merge Sort and Quick Sort',
            'Both sorts split the problem in half, but one guarantees the split and the other gambles on the pivot.',
            'Merge sort is predictable and stable at the cost of extra space; quick sort is in place and faster in practice with a decent pivot.',
            ['Implement both sorts from scratch', 'Explain stability and its practical effect', 'Describe quick sort worst-case behaviour'],
            'Merge sort logic is also how you count inversions — the same pass that merges can tally out-of-order pairs for free.',
            'merge sort quick sort algorithm explained tutorial'),
          l('sliding-window', 'The Sliding Window',
            'A sliding window keeps a running answer for a contiguous range, replacing a recomputation for every subarray with one pass.',
            'The window grows and shrinks according to a constraint, updating an aggregate rather than rebuilding it each time.',
            ['Apply a fixed and a variable window', 'Maintain an aggregate incrementally', 'Handle constraints that force a shrink'],
            'The window only works if removing from the left is as cheap as adding to the right. If it is not, you need a different structure.',
            'sliding window technique algorithm tutorial'),
        ]),
      m(
        'Dynamic programming',
        'Overlapping subproblems and optimal substructure, from a naive recursion through memoisation to a bottom-up table.',
        [
          l('memoization-to-tabulation', 'From Memoisation to Tabulation',
            'Dynamic programming is recursion that stops recomputing, and tabulation is the same recursion with the order made explicit.',
            'You define a state, write the recurrence, cache it, and then fill a table in dependency order to drop the call stack.',
            ['Define a state and a recurrence', 'Convert a recursion into a memoised one', 'Rewrite a memoised solution bottom-up'],
            'Get the recursion right before optimising it. A table built from a wrong recurrence is just a faster wrong answer.',
            'dynamic programming memoization tabulation tutorial'),
          l('knapsack-problems', 'Knapsack Problems',
            'The knapsack is the archetype for choosing a subset under a capacity constraint, and half of DP problems are it in disguise.',
            'The state is item index and remaining capacity, and each step is a take-it-or-leave-it decision.',
            ['Solve the 0/1 knapsack', 'Adapt it to unbounded and subset-sum variants', 'Reduce the table to one dimension'],
            'When you compress the table to one row, iterate capacity downwards for 0/1 and upwards for unbounded. The direction is the variant.',
            'knapsack problem dynamic programming tutorial'),
          l('lcs-and-edit-distance', 'Longest Common Subsequence and Edit Distance',
            'String DP compares two sequences position by position, and the whole family shares one two-dimensional table.',
            'Each cell depends on its neighbours, with a match extending a subsequence and a mismatch taking the best repair.',
            ['Build a two-dimensional string DP table', 'Reconstruct the answer, not just its length', 'Relate edit distance to sequence alignment'],
            'To reconstruct the actual sequence, walk the table backwards from the final cell — no need to store parent pointers.',
            'longest common subsequence edit distance dynamic programming'),
        ]),
      m(
        'Graph algorithms',
        'Weighted shortest paths, ordering a dependency graph, and the near-constant-time structure behind connectivity.',
        [
          l('dijkstra-shortest-path', 'Dijkstra Shortest Paths',
            'Dijkstra finds shortest paths when edge weights are non-negative, expanding always from the closest unfinished node.',
            'A priority queue supplies that node, and each pop finalises a distance that can never improve later.',
            ['Implement Dijkstra with a priority queue', 'Explain why negative weights break it', 'Reconstruct the path, not just the distance'],
            'Skip a popped node whose recorded distance is already better than the entry you popped — that is the lazy-deletion trick.',
            'dijkstra algorithm shortest path priority queue tutorial'),
          l('topological-sort', 'Topological Sort',
            'A topological order lists nodes so every edge points forward, which is what a build system or scheduler needs.',
            'Kahn algorithm repeatedly removes a node with no remaining prerequisites, and a leftover node proves there is a cycle.',
            ['Order a dependency graph', 'Detect a cycle during sorting', 'Apply topological order to DP on a DAG'],
            'If the sort ends with nodes unprocessed, you have found a dependency cycle — the same run detects it for free.',
            'topological sort kahn algorithm tutorial'),
          l('union-find', 'Union-Find',
            'Union-find answers "are these two in the same group" in nearly constant time, which underpins Kruskal and connectivity problems.',
            'Path compression flattens the tree during lookup and union by rank keeps it shallow to begin with.',
            ['Implement find and union', 'Apply path compression and union by rank', 'Use it for connectivity and cycle detection'],
            'Without both optimisations it degrades to a linked list. They are ten lines together and they are not optional.',
            'union find disjoint set path compression tutorial'),
        ]),
      m(
        'Interview craft',
        'Exhaustive search done well, greedy choices you can actually justify, and communicating complexity while under pressure.',
        [
          l('backtracking', 'Backtracking',
            'Backtracking explores choices depth-first and undoes them on the way out, which is how permutations and constraint puzzles are solved.',
            'Pruning an infeasible branch early is what separates a solution that finishes from one that does not.',
            ['Structure a choose-explore-unchoose loop', 'Prune branches that cannot succeed', 'Estimate the size of a search space'],
            'Undo the choice on the way out. A shared mutable state that is never restored is the single most common backtracking bug.',
            'backtracking algorithm permutations n queens tutorial'),
          l('greedy-and-exchange-arguments', 'Greedy Algorithms and Exchange Arguments',
            'A greedy algorithm commits to the locally best choice, which is either optimal or badly wrong, with little in between.',
            'An exchange argument proves optimality by showing any other solution can be transformed into the greedy one without getting worse.',
            ['Identify when greedy is safe', 'Prove a greedy choice with an exchange argument', 'Find a counterexample when it is not'],
            'Try to break your greedy rule with a small counterexample before you trust it. Two minutes there saves a wrong submission.',
            'greedy algorithm exchange argument proof tutorial'),
          l('complexity-under-pressure', 'Complexity Under Pressure',
            'Stating complexity clearly is part of the answer, and the constraints in the problem usually tell you which one is expected.',
            'Working backwards from the input size to a target complexity narrows the approach before you write a line.',
            ['Infer the expected complexity from constraints', 'State time and space out loud', 'Decide when to optimise and when to stop'],
            'An input of a million rules out anything quadratic. The constraints are a hint about the intended algorithm, not decoration.',
            'algorithm interview time complexity analysis tips'),
        ]),
    ],
  },

  {
    slug: 'machine-learning-foundations',
    title: 'Machine Learning Foundations',
    summary:
      'Machine learning from the arithmetic outwards: the linear algebra and probability underneath, the classic supervised models, honest evaluation, and the first neural network you train yourself.',
    instructor: 'hana-sato',
    category: 'ai-and-machine-learning',
    level: 'beginner',
    price: 99,
    popular: true,
    outcomes: [
      ['chart', 'Understand the maths', 'Work comfortably with the vectors, matrices and probability a model is built from.'],
      ['layers', 'Train classic models', 'Fit and interpret regression, classification and tree-based models.'],
      ['shield', 'Evaluate honestly', 'Validate properly and choose a metric that matches the decision being made.'],
      ['bolt', 'Build a neural network', 'Go from a single neuron to a trained network with backpropagation.'],
    ],
    docs: [
      ['documentation', 'scikit-learn user guide', 'The library used for the supervised learning and evaluation modules.', 'https://scikit-learn.org/stable/user_guide.html'],
      ['documentation', 'PyTorch tutorials', 'Reference for the neural network module.', 'https://pytorch.org/tutorials/'],
    ],
    modules: [
      m(
        'Maths and data',
        'The vector and probability groundwork, and the unglamorous data work that decides more outcomes than model choice does.',
        [
          l('linear-algebra-for-ml', 'Linear Algebra for Machine Learning',
            'A dataset is a matrix and a model is largely matrix multiplication, so the notation is worth reading fluently.',
            'Vectors represent examples and features, matrix products apply transformations, and dimensions are the first thing to check.',
            ['Read vector and matrix notation confidently', 'Multiply matrices and check shapes', 'Interpret a dot product geometrically'],
            'Most early bugs are shape errors. Write the expected dimensions beside each line and the mistake becomes obvious.',
            'linear algebra for machine learning vectors matrices tutorial'),
          l('probability-and-statistics', 'Probability and Statistics',
            'Models output likelihoods and datasets are samples, so probability is the language for both prediction and uncertainty.',
            'Distributions describe variation, conditional probability underpins classification, and sampling explains why estimates wobble.',
            ['Work with common distributions', 'Apply conditional probability and Bayes rule', 'Distinguish a sample from a population'],
            'A difference measured on a small sample often is not a difference at all. Ask about variance before celebrating a metric.',
            'probability statistics for machine learning tutorial'),
          l('data-cleaning-and-features', 'Data Cleaning and Feature Engineering',
            'Feature quality sets the ceiling on model quality, and most real datasets arrive missing, skewed or inconsistently encoded.',
            'Handling missing values, scaling and encoding categories are the steps that make a model trainable at all.',
            ['Handle missing and outlier values', 'Scale and encode features appropriately', 'Avoid leaking the target into a feature'],
            'Fit every transformation on the training split only. Scaling with statistics from the whole dataset leaks the test set.',
            'data cleaning feature engineering machine learning tutorial'),
        ]),
      m(
        'Supervised learning',
        'The three model families worth knowing before anything else: linear regression, classification, and trees.',
        [
          l('linear-regression', 'Linear Regression',
            'Linear regression fits a straight relationship by minimising squared error, and it is still the right answer surprisingly often.',
            'Coefficients are learned by gradient descent or closed form, and each one reads as an effect per unit of its feature.',
            ['Fit and interpret a linear model', 'Minimise a loss with gradient descent', 'Check residuals for broken assumptions'],
            'Plot the residuals. A clear pattern in them means the relationship is not linear and a better score will not save you.',
            'linear regression machine learning gradient descent tutorial'),
          l('logistic-regression', 'Logistic Regression and Classification',
            'Logistic regression predicts a probability rather than a value, which makes it a classifier once you choose a threshold.',
            'A sigmoid squashes the linear output into zero-to-one, and cross-entropy loss penalises confident mistakes hardest.',
            ['Predict calibrated probabilities', 'Choose a decision threshold deliberately', 'Interpret coefficients as odds'],
            'The default 0.5 threshold is a choice, not a law. Set it from the cost of a false positive versus a false negative.',
            'logistic regression classification machine learning tutorial'),
          l('decision-trees-and-forests', 'Decision Trees and Random Forests',
            'A tree splits data on one feature at a time, producing a model you can read, and an ensemble of them fixes its instability.',
            'Each split maximises purity, an unconstrained tree memorises the training set, and averaging many trees restores generalisation.',
            ['Train and read a decision tree', 'Control depth to limit overfitting', 'Explain why a forest beats a single tree'],
            'Feature importances from a forest are suggestive, not causal. A high-importance feature may just be a proxy for the target.',
            'decision trees random forest machine learning tutorial'),
        ]),
      m(
        'Model quality',
        'Validation, the bias-variance trade-off, and choosing a metric that reflects the decision the model is actually supporting.',
        [
          l('train-test-split-and-cross-validation', 'Validation and Cross-Validation',
            'A score on data the model has seen is meaningless, so evaluation depends entirely on how you hold data out.',
            'Cross-validation rotates the held-out fold to use all the data for both roles and report a more stable estimate.',
            ['Split data without leaking', 'Run k-fold cross-validation', 'Keep a final test set untouched until the end'],
            'For time series, split by time. A random split lets the model learn from the future, and the score will look wonderful.',
            'cross validation train test split machine learning tutorial'),
          l('bias-variance-and-regularization', 'Bias, Variance and Regularisation',
            'Underfitting and overfitting are two failures of the same trade-off, and regularisation is the dial between them.',
            'Penalising large coefficients constrains the model, with one penalty shrinking weights and another driving them to zero.',
            ['Diagnose underfitting versus overfitting', 'Apply L1 and L2 regularisation', 'Read a learning curve'],
            'A large gap between training and validation score means variance; both being bad means bias. The fix differs completely.',
            'bias variance tradeoff regularization machine learning explained'),
          l('classification-metrics', 'Classification Metrics',
            'Accuracy hides everything on an imbalanced dataset, where predicting the majority class always looks excellent.',
            'Precision and recall separate the two error types, and a ROC or PR curve shows behaviour across every threshold.',
            ['Read a confusion matrix', 'Trade precision against recall', 'Choose a metric that matches the cost of an error'],
            'On a 99-to-1 dataset, 99% accuracy is the do-nothing baseline. Always compare against the trivial model.',
            'precision recall f1 roc auc classification metrics explained'),
        ]),
      m(
        'Neural networks',
        'From a single neuron to a trained multi-layer network, with the training loop written out rather than assumed.',
        [
          l('perceptron-to-mlp', 'From Perceptron to Multi-Layer Network',
            'A single neuron is a weighted sum and a non-linearity; stacking them is what lets a network represent curved boundaries.',
            'Layers transform the representation step by step, and without a non-linear activation the whole stack collapses to one linear map.',
            ['Compute a forward pass by hand', 'Explain why activations must be non-linear', 'Choose a sensible layer and width structure'],
            'Remove the activations and a deep network is exactly equivalent to a single linear layer. That is the whole reason they are there.',
            'neural network perceptron multilayer explained tutorial'),
          l('backpropagation-and-gradient-descent', 'Backpropagation and Gradient Descent',
            'Backpropagation is the chain rule applied backwards through the network, assigning each weight its share of the error.',
            'Gradients flow from the loss to every parameter, and the optimiser takes a step whose size the learning rate controls.',
            ['Derive a gradient through a small network', 'Explain the role of the learning rate', 'Recognise vanishing or exploding gradients'],
            'If the loss diverges immediately, lower the learning rate before changing anything else. It is the usual culprit.',
            'backpropagation gradient descent neural network explained'),
          l('training-with-pytorch', 'Training a Network with PyTorch',
            'PyTorch handles the gradients so your job is the model definition, the data pipeline and an honest training loop.',
            'Tensors track operations for automatic differentiation, and each loop step runs forward, computes loss, backpropagates and steps.',
            ['Define a model and a training loop', 'Batch data with a data loader', 'Track training and validation loss per epoch'],
            'Zero the gradients every step. PyTorch accumulates them by default, and forgetting produces a model that trains nonsense.',
            'pytorch training loop neural network tutorial'),
        ]),
    ],
  },

  {
    slug: 'building-llm-applications',
    title: 'Building LLM Applications',
    summary:
      'Ship something real on top of a language model: control the context window, get structured output you can trust, build retrieval that actually retrieves, add tools, and evaluate the result instead of demoing it.',
    instructor: 'hana-sato',
    category: 'ai-and-machine-learning',
    level: 'intermediate',
    price: 119,
    popular: true,
    outcomes: [
      ['terminal', 'Control the context', 'Budget tokens and structure a prompt for a predictable answer.'],
      ['layers', 'Build retrieval', 'Chunk, embed and search your own content so answers are grounded in it.'],
      ['bolt', 'Add tools and agents', 'Let a model call functions and run a loop that terminates.'],
      ['chart', 'Evaluate and operate', 'Measure quality, cost and latency, and put guardrails around the output.'],
    ],
    docs: [
      ['documentation', 'Claude API documentation', 'Messages, tool use and structured output for the modules in this course.', 'https://docs.anthropic.com/en/api/overview'],
      ['documentation', 'Model Context Protocol', 'The open protocol covered in the agents module.', 'https://modelcontextprotocol.io/'],
    ],
    modules: [
      m(
        'Working with language models',
        'What the model actually receives, how to shape it, and how to get output a program can parse.',
        [
          l('tokens-and-context-windows', 'Tokens and Context Windows',
            'A model reads tokens, not characters, and the context window is a hard budget shared by your prompt and its answer.',
            'Tokenisation splits text into subword pieces, and cost and latency both scale with how many of them you send.',
            ['Estimate the token cost of a prompt', 'Budget context between instructions and data', 'Truncate or summarise long inputs deliberately'],
            'Never send a whole document when a filtered extract will do. Most context-window failures are a query that fetched too much.',
            'llm tokens context window explained tutorial'),
          l('prompt-engineering', 'Prompt Engineering',
            'A prompt is a specification, and most disappointing output is an under-specified one rather than a model limitation.',
            'Clear roles, explicit constraints and worked examples move a model from plausible output to the output you asked for.',
            ['Write instructions the model can follow exactly', 'Use examples to pin down a format', 'Iterate on a prompt with a fixed test set'],
            'Change one thing at a time and re-run the same inputs. Without a fixed set, prompt tuning is just moving failures around.',
            'prompt engineering techniques llm tutorial'),
          l('structured-output', 'Structured Output',
            'An application needs parseable data, not prose, and asking politely for JSON is not the same as guaranteeing it.',
            'A schema constrains the shape of the response, and validating on arrival turns a bad generation into a handled error.',
            ['Request output against a schema', 'Validate a response before using it', 'Handle a malformed or refused generation'],
            'Validate with the same schema you sent. Trusting the shape because you asked for it is how a parser crashes in production.',
            'llm structured output json schema function calling tutorial'),
        ]),
      m(
        'Retrieval and RAG',
        'Grounding answers in your own content: embeddings, chunking, and the pipeline that ties them to a generation step.',
        [
          l('embeddings-and-vector-search', 'Embeddings and Vector Search',
            'An embedding places text in a space where nearby vectors mean similar things, so search can match meaning rather than words.',
            'A vector index stores those embeddings and returns the nearest neighbours to a query vector.',
            ['Explain what an embedding vector represents', 'Search by cosine similarity', 'Choose between keyword and vector search'],
            'Vector search finds similar, not correct. For exact identifiers and names, keyword matching still wins outright.',
            'embeddings vector search similarity explained tutorial'),
          l('chunking-strategies', 'Chunking Strategies',
            'Retrieval quality is mostly a chunking problem: chunks too large bury the answer, too small and they lose their context.',
            'Splitting on structure rather than character count keeps a chunk self-contained, and overlap protects an answer at a boundary.',
            ['Split content along its structure', 'Pick a chunk size and overlap', 'Keep metadata attached to each chunk'],
            'Store the heading path with each chunk. It is the cheapest context you can add and it improves ranking noticeably.',
            'rag chunking strategies document splitting tutorial'),
          l('building-a-rag-pipeline', 'Building a RAG Pipeline',
            'A retrieval pipeline fetches relevant context, puts it in the prompt, and asks the model to answer only from it.',
            'Ingestion, retrieval and generation are separate stages, and a bad answer is usually a retrieval failure, not a generation one.',
            ['Assemble ingestion, retrieval and generation', 'Ground an answer and cite its sources', 'Debug whether retrieval or generation failed'],
            'Log the retrieved chunks with every answer. Without them you cannot tell a hallucination from a correct read of bad context.',
            'building rag pipeline retrieval augmented generation tutorial'),
        ]),
      m(
        'Tools and agents',
        'Giving a model the ability to act, keeping the loop bounded, and standardising how tools are exposed.',
        [
          l('tool-calling', 'Tool and Function Calling',
            'Tool calling lets a model request a function with structured arguments, so it can look things up instead of inventing them.',
            'You describe each tool, the model chooses one and supplies arguments, your code runs it and returns the result.',
            ['Define a tool the model can use', 'Execute a call and return its result', 'Validate arguments before acting on them'],
            'A tool description is a prompt. Vague parameter descriptions are the main reason a model calls the wrong tool.',
            'llm tool calling function calling tutorial'),
          l('agent-loops', 'Agent Loops',
            'An agent alternates between thinking and calling tools until it has an answer, which is powerful and easy to leave unbounded.',
            'Each iteration adds observations to the context, so step limits and termination conditions are a design requirement.',
            ['Structure a think-act-observe loop', 'Bound iterations and cost', 'Recover from a failed tool call'],
            'Cap the number of steps and the total tokens. An agent with no ceiling will happily spend your budget in a retry loop.',
            'llm agent loop react pattern tutorial'),
          l('model-context-protocol', 'The Model Context Protocol',
            'MCP standardises how a model reaches tools and data, so an integration is not rewritten for every client.',
            'A server exposes tools and resources over a defined protocol, and any compatible client can discover and call them.',
            ['Explain what an MCP server exposes', 'Connect a client to a server', 'Decide what belongs in a tool versus a prompt'],
            'Connect to an MCP server from the server side. A browser holding those credentials is the same mistake as a client-side token.',
            'model context protocol mcp explained tutorial'),
        ]),
      m(
        'Production LLM systems',
        'Evaluation, the cost and latency budget, and the guardrails that decide what is allowed to reach a user.',
        [
          l('evals', 'Evaluating LLM Output',
            'Without an eval set you are tuning on vibes, and every prompt change is an unmeasured trade.',
            'A fixed set of inputs with expected properties gives a score you can compare across versions, whether judged by code or a model.',
            ['Build an eval set from real failures', 'Score output automatically', 'Compare two prompt versions honestly'],
            'Grow the eval set from production failures. Every bug you fix should leave a test case behind that proves it stays fixed.',
            'llm evaluation evals testing tutorial'),
          l('cost-and-latency', 'Cost and Latency',
            'Tokens are the unit of both the bill and the wait, which makes context size the main lever on each.',
            'Caching a stable prefix, streaming the response and choosing a smaller model per task all cut one or both.',
            ['Measure cost per request', 'Cache a stable prompt prefix', 'Stream output to cut perceived latency'],
            'Put everything stable at the front of the prompt. A cached prefix is the cheapest speed-up available, and it is nearly free.',
            'llm cost optimization latency prompt caching tutorial'),
          l('guardrails-and-safety', 'Guardrails and Safety',
            'Model output is untrusted input to your application, and so is any text it retrieved on the way.',
            'Input filtering, output validation and clear refusal behaviour keep an application predictable when a request is not.',
            ['Validate output before it is displayed or executed', 'Defend against prompt injection in retrieved content', 'Define refusal behaviour deliberately'],
            'Retrieved documents can carry instructions. Treat every retrieved chunk as data the model may quote, never as orders it may follow.',
            'llm guardrails prompt injection safety tutorial'),
        ]),
    ],
  },

  {
    slug: 'system-design-for-scale',
    title: 'System Design for Scale',
    summary:
      'Design systems that survive traffic and failure: load balancing and consistency, caching and sharding, the coordination problems between services, and how to structure a design interview answer.',
    instructor: 'tobi-lindqvist',
    category: 'backend-and-systems',
    level: 'advanced',
    price: 109,
    popular: false,
    outcomes: [
      ['layers', 'Scale deliberately', 'Choose between vertical and horizontal scaling and know what each costs.'],
      ['bolt', 'Handle data at volume', 'Apply caching, sharding, replication and queues where they belong.'],
      ['shield', 'Design for failure', 'Build in rate limiting, retries and idempotency before the incident.'],
      ['rocket', 'Interview well', 'Drive a design discussion from requirements to trade-offs without rambling.'],
    ],
    docs: [
      ['repository', 'System design primer', 'A broad reference for the topics covered across these modules.', 'https://github.com/donnemartin/system-design-primer'],
      ['article', 'Designing data-intensive applications notes', 'Companion reading on replication, partitioning and consistency.', 'https://dataintensive.net/'],
    ],
    modules: [
      m(
        'Scaling fundamentals',
        'What happens when one machine is not enough: distributing traffic, and the consistency you give up by doing so.',
        [
          l('scalability-basics', 'Scalability Basics',
            'Scaling up buys a bigger machine and scaling out buys more of them, and only one of those has a ceiling you can afford.',
            'Horizontal scaling requires statelessness, which is why session state has to move out of the application process.',
            ['Compare vertical and horizontal scaling', 'Make a service stateless', 'Find the bottleneck before adding machines'],
            'Adding servers in front of one overloaded database just moves the queue. Find the real bottleneck before you scale anything.',
            'system design scalability horizontal vertical scaling explained'),
          l('load-balancing', 'Load Balancing',
            'A load balancer spreads requests across instances and stops sending traffic to the ones that are unhealthy.',
            'Routing algorithms differ in how they account for load, and health checks decide who stays in the pool.',
            ['Compare round-robin, least-connections and hashing', 'Configure health checks that mean something', 'Handle sticky sessions or remove the need for them'],
            'A health check that only proves the process is alive will keep routing to an instance whose database connection died.',
            'load balancing algorithms system design explained'),
          l('cap-and-consistency', 'CAP and Consistency Models',
            'A distributed system that partitions must choose between staying consistent and staying available, and the choice is per feature.',
            'Strong consistency makes every read see the last write, while eventual consistency trades that for availability and latency.',
            ['State the CAP trade-off precisely', 'Choose a consistency model per use case', 'Explain read-your-writes and monotonic reads'],
            'Pick consistency per operation. A like count can be eventually consistent; a payment balance cannot.',
            'cap theorem consistency models distributed systems explained'),
        ]),
      m(
        'Data at scale',
        'Keeping reads fast, spreading data across machines, and decoupling services with a queue.',
        [
          l('caching-strategies', 'Caching Strategies',
            'A cache trades freshness for speed, and every caching bug is really a question about when the stale copy dies.',
            'Read-through, write-through and write-behind differ in who populates the cache and when, and eviction decides what survives.',
            ['Choose a caching pattern', 'Set invalidation and TTL deliberately', 'Avoid a stampede when a hot key expires'],
            'Add jitter to TTLs. Keys that expire together cause a thundering herd against the database at exactly the wrong moment.',
            'caching strategies system design cache invalidation explained'),
          l('sharding-and-replication', 'Sharding and Replication',
            'Replication copies data for availability and read capacity; sharding splits it so no single machine holds it all.',
            'The shard key decides distribution, and a poorly chosen one concentrates traffic on one partition.',
            ['Choose a shard key that spreads load', 'Compare replication topologies', 'Handle cross-shard queries and rebalancing'],
            'A shard key you cannot change later is a permanent decision. Resharding a live system is one of the worst weeks available.',
            'database sharding replication system design explained'),
          l('message-queues', 'Message Queues',
            'A queue decouples producer from consumer, absorbing bursts and letting slow work happen out of the request path.',
            'Delivery is usually at-least-once, so consumers have to tolerate duplicates, and dead-letter queues catch what keeps failing.',
            ['Move slow work out of the request path', 'Design for at-least-once delivery', 'Use a dead-letter queue for poison messages'],
            'Assume every message can arrive twice. Design the consumer to be idempotent rather than hoping the broker is exactly-once.',
            'message queue kafka rabbitmq system design explained'),
        ]),
      m(
        'Distributed systems concerns',
        'Service boundaries, protecting a service from too much traffic, and making retries safe.',
        [
          l('microservices-vs-monolith', 'Microservices versus Monolith',
            'Splitting a system into services buys independent deployment and costs you a network between every function call.',
            'Service boundaries should follow ownership and change frequency, not a diagram of nouns.',
            ['Weigh the real cost of a service boundary', 'Split along ownership lines', 'Recognise a distributed monolith'],
            'If two services always deploy together, they are one service in an expensive costume. Merge them.',
            'microservices vs monolith architecture trade-offs explained'),
          l('rate-limiting', 'Rate Limiting',
            'Rate limiting protects a service from traffic it cannot serve, whether that traffic is malicious or merely enthusiastic.',
            'Token bucket and sliding window differ in how they treat bursts, and distributed limiting needs shared state.',
            ['Compare token bucket and sliding window', 'Limit per user, per key and globally', 'Return limits clients can actually respect'],
            'Send the limit and reset time in the response headers. A client that cannot see the limit will simply retry into it.',
            'rate limiting algorithms token bucket system design'),
          l('idempotency-and-retries', 'Idempotency and Retries',
            'Retries turn a transient failure into a success and a non-idempotent operation into a duplicate charge.',
            'An idempotency key lets the server recognise a repeat, and backoff with jitter keeps retries from synchronising.',
            ['Make a write safely repeatable', 'Apply exponential backoff with jitter', 'Use a circuit breaker to stop retrying a dead service'],
            'Retries without jitter create a synchronised stampede — every client retrying at the same instant is a second outage.',
            'idempotency retries exponential backoff circuit breaker explained'),
        ]),
      m(
        'Design interviews',
        'Three canonical problems, each worked from requirements through data model to bottleneck.',
        [
          l('design-a-url-shortener', 'Design a URL Shortener',
            'A URL shortener looks trivial and immediately raises key generation, read-heavy caching and storage growth.',
            'Short codes come from a counter or a hash, reads dominate writes enormously, and expiry decides how storage behaves over years.',
            ['Generate short keys without collisions', 'Design for a read-heavy workload', 'Estimate storage and traffic from requirements'],
            'Start with the read-to-write ratio. It is roughly a thousand to one here, and it dictates the entire design.',
            'system design interview url shortener tutorial'),
          l('design-a-news-feed', 'Design a News Feed',
            'A feed forces a choice between assembling on read and pushing on write, and celebrity accounts break the naive answer.',
            'Fan-out on write makes reads cheap but explodes for large followings, while fan-out on read moves the cost to every view.',
            ['Compare fan-out on write and on read', 'Handle the celebrity problem with a hybrid', 'Rank and paginate a feed consistently'],
            'Hybrid is the accepted answer: push for ordinary accounts, pull for the few with millions of followers.',
            'system design interview news feed design tutorial'),
          l('design-a-chat-system', 'Design a Chat System',
            'Chat needs a persistent connection, ordered delivery and message history, which is three different hard problems at once.',
            'WebSockets hold the connection, a per-conversation sequence keeps order, and presence is its own subsystem.',
            ['Choose a real-time transport', 'Guarantee ordering and delivery receipts', 'Store and paginate message history'],
            'Order messages by a server-assigned sequence per conversation. Client clocks disagree and users notice immediately.',
            'system design interview chat system whatsapp design tutorial'),
        ]),
    ],
  },

  {
    slug: 'sql-and-database-performance',
    title: 'SQL and Database Performance',
    summary:
      'Write SQL that stays fast as the table grows: joins and window functions, how indexes really work, reading a query plan, and changing a live schema without taking the service down.',
    instructor: 'elena-vasquez',
    category: 'data-and-databases',
    level: 'intermediate',
    price: 79,
    popular: false,
    outcomes: [
      ['terminal', 'Write real SQL', 'Use joins, aggregation and window functions with confidence.'],
      ['bolt', 'Index correctly', 'Design indexes that the planner will actually use.'],
      ['chart', 'Read query plans', 'Diagnose a slow query from its plan rather than by guessing.'],
      ['shield', 'Operate safely', 'Handle transactions, locking and migrations without downtime.'],
    ],
    docs: [
      ['documentation', 'PostgreSQL documentation', 'The reference used throughout for syntax, indexes and planning.', 'https://www.postgresql.org/docs/current/index.html'],
      ['article', 'Use the index, Luke', 'A practical guide to indexing and query tuning.', 'https://use-the-index-luke.com/'],
    ],
    modules: [
      m(
        'SQL fundamentals',
        'Combining tables, summarising rows, and the window functions that answer questions a GROUP BY cannot.',
        [
          l('joins', 'Joins in Practice',
            'A join matches rows across tables, and the join type decides what happens to rows with no match on the other side.',
            'Inner keeps only matches, outer keeps unmatched rows with nulls, and a missing condition produces a cross product.',
            ['Choose the right join type', 'Join on multiple conditions correctly', 'Recognise an accidental cross join'],
            'A result larger than both inputs means your join condition is not unique on at least one side. Check before you trust it.',
            'sql joins inner left outer explained tutorial'),
          l('aggregation-and-group-by', 'Aggregation and GROUP BY',
            'Aggregation collapses rows into summaries, and every non-aggregated column must be part of the grouping.',
            'HAVING filters after grouping while WHERE filters before it, which changes both the result and the cost.',
            ['Group and aggregate correctly', 'Filter before and after aggregation', 'Handle nulls in aggregate functions'],
            'Filter in WHERE whenever you can. Anything you push into HAVING was aggregated first and then thrown away.',
            'sql group by aggregation having tutorial'),
          l('window-functions', 'Window Functions',
            'A window function computes across related rows while keeping every row, which is what running totals and rankings need.',
            'The partition defines the group, the ordering defines the sequence, and the frame defines how many rows are in view.',
            ['Rank rows within a partition', 'Compute running totals and moving averages', 'Compare a row to its neighbours'],
            'Window functions run after WHERE. To filter on a rank you have to wrap the query in a subquery or CTE.',
            'sql window functions over partition by tutorial'),
        ]),
      m(
        'Indexing',
        'What an index physically is, how column order changes everything, and the cases where adding one makes things worse.',
        [
          l('btree-indexes', 'How B-Tree Indexes Work',
            'A B-tree index is a sorted structure the database can descend, turning a full scan into a handful of page reads.',
            'Lookups follow the tree to a leaf, and the same ordering also serves range scans and ORDER BY for free.',
            ['Explain how a lookup descends the tree', 'Predict when an index serves a range query', 'Know why a leading wildcard cannot use one'],
            'A function around an indexed column usually disables the index. Index the expression instead, or rewrite the predicate.',
            'database b-tree index how it works explained'),
          l('composite-and-covering-indexes', 'Composite and Covering Indexes',
            'In a multi-column index the column order decides which queries can use it, and the wrong order makes it dead weight.',
            'A query can use a leading prefix of the index, and a covering index answers entirely from the index without touching the table.',
            ['Order columns for the queries you run', 'Use the leftmost-prefix rule', 'Cover a query to avoid table lookups'],
            'Equality columns first, then the range column. A range early in the index stops every column after it from filtering.',
            'composite index covering index column order sql tutorial'),
          l('when-indexes-hurt', 'When Indexes Hurt',
            'Every index is maintained on every write, so an unused one costs continuously and returns nothing.',
            'Low-selectivity indexes get ignored by the planner, and redundant ones duplicate the maintenance for no benefit.',
            ['Find unused and duplicate indexes', 'Judge selectivity before adding one', 'Weigh write cost against read gain'],
            'Check index usage statistics before adding another. Most slow-write complaints are a table carrying indexes nobody queries.',
            'database index write performance unused indexes explained'),
        ]),
      m(
        'Query tuning',
        'Finding out what the database is actually doing, and fixing the access patterns that do not survive growth.',
        [
          l('explain-plans', 'Reading EXPLAIN Plans',
            'The query plan shows what the database intends to do, which is frequently not what you assumed.',
            'Each node reports an access method and an estimated cost, and a large gap between estimate and reality points at stale statistics.',
            ['Read a plan from the leaves up', 'Spot a sequential scan that should be an index scan', 'Compare estimated with actual rows'],
            'A wildly wrong row estimate usually means stale statistics. Analyse the table before you start rewriting the query.',
            'sql explain analyze query plan tutorial postgres'),
          l('n-plus-one-queries', 'N+1 Queries and Batch Fetching',
            'An N+1 pattern runs one query for a list and then one more per row, which looks fine locally and collapses in production.',
            'Batching the follow-up into a single query with an IN list or a join replaces hundreds of round trips with one.',
            ['Recognise an N+1 in application logs', 'Batch related lookups into one query', 'Choose between a join and a second batched query'],
            'Count the queries per request in development. An ORM makes N+1 invisible in the code and obvious in the log.',
            'n+1 query problem batch loading orm explained'),
          l('pagination-at-scale', 'Pagination at Scale',
            'Offset pagination gets slower the deeper you go, because the database still walks every row it skips.',
            'Keyset pagination seeks straight to the last key seen, keeping every page the same cost as the first.',
            ['Explain why deep offsets are slow', 'Implement keyset pagination', 'Keep ordering stable across pages'],
            'Include a unique tiebreaker in the sort. Without one, rows can repeat or vanish between pages when values tie.',
            'keyset pagination cursor pagination sql offset slow'),
        ]),
      m(
        'Operating a database',
        'Transactions and what isolation actually promises, the locks behind a stalled query, and shipping a schema change safely.',
        [
          l('transactions-and-isolation', 'Transactions and Isolation Levels',
            'A transaction makes several statements atomic, and the isolation level decides which anomalies are still possible.',
            'Read committed, repeatable read and serialisable trade concurrency for guarantees, each preventing a different anomaly.',
            ['Explain the ACID properties concretely', 'Choose an isolation level for a workload', 'Identify dirty, non-repeatable and phantom reads'],
            'Read committed is the common default and it still allows non-repeatable reads. Check the default before assuming safety.',
            'database transactions acid isolation levels explained'),
          l('locking-and-deadlocks', 'Locking and Deadlocks',
            'Locks serialise conflicting access, and a deadlock is two transactions each holding what the other needs next.',
            'Lock granularity and ordering determine contention, and the database resolves a deadlock by killing one participant.',
            ['Diagnose a blocked query', 'Prevent deadlocks with consistent lock ordering', 'Keep transactions short to reduce contention'],
            'Always touch rows in the same order across transactions. Inconsistent ordering is the cause of nearly every deadlock.',
            'database locking deadlock explained postgres mysql'),
          l('zero-downtime-migrations', 'Migrations Without Downtime',
            'A schema change on a large table can lock it, and a migration that blocks writes is an outage with a ticket number.',
            'Expand-and-contract rolls a change out in compatible steps so old and new application versions can both run.',
            ['Apply the expand-and-contract pattern', 'Add columns and indexes without long locks', 'Backfill large tables in batches'],
            'Backfill in batches with a pause between them. One enormous UPDATE holds locks and bloats the write-ahead log.',
            'zero downtime database migration expand contract tutorial'),
        ]),
    ],
  },
]
